# C언어 콘솔 방향키 이동 프로그램 (Console Arrow Key Movement)

Windows 콘솔 환경에서 방향키(`↑`, `↓`, `←`, `→`)를 이용해 문자(`'A'`)를 실시간으로 이동시키는 인터랙티브 C언어 프로그램입니다.

---

## 🕹️ 조작법 및 기능
* **방향키 (`↑`, `↓`, `←`, `→`)**: 문자 'A'를 상하좌우로 이동시킵니다. (콘솔 창 크기 경계 제한 적용)
* **`Esc` 키 (아스키코드 27)**: 프로그램을 종료합니다.
